rootProject.name = "MyApp"
include(":app")
# Enable AndroidX (required when using androidx.* libraries)
android.useAndroidX=true
# Enable Jetifier to rewrite old support-library references to AndroidX on the fly
android.enableJetifier=true
