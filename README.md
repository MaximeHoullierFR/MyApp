```markdown
# MyApp - HelloWorld Android

This PR adds a minimal Android (Kotlin) "Hello World" app and a GitHub Actions workflow that builds the debug APK and uploads it as an artifact.

How to build locally
1. Open the project in Android Studio (recommended) or run `./gradlew assembleDebug`.

CI
- The included workflow builds the debug APK and uploads it as an artifact.
```
